[project]
name = "majoor-assetsmanager"
description = "Advanced asset browser for ComfyUI outputs (images/videos) with ratings & tags, full-text search, workflow fingerprinting, and advanced metadata management."
version = "2.1.0"
license = {file = "LICENSE"} 
# classifiers = [
#     # For OS-independent nodes (works on all operating systems)
#     "Operating System :: OS Independent",
#
#     # OR for OS-specific nodes, specify the supported systems:
#     "Operating System :: Microsoft :: Windows",  # Windows specific
#     "Operating System :: POSIX :: Linux",  # Linux specific
#     "Operating System :: MacOS",  # macOS specific
#
#     # GPU Accelerator support. Pick the ones that are supported by your extension.
#     "Environment :: GPU :: NVIDIA CUDA",    # NVIDIA CUDA support
#     "Environment :: GPU :: AMD ROCm",       # AMD ROCm support
#     "Environment :: GPU :: Intel Arc",      # Intel Arc support
#     "Environment :: NPU :: Huawei Ascend",  # Huawei Ascend support
#     "Environment :: GPU :: Apple Metal",    # Apple Metal support
# ]

dependencies = ["# ComfyUI-Majoor-AssetsManager Python Dependencies", "# Core web framework", "aiohttp>=3.8.0", "# Async SQLite (non-blocking DB adapter)", "aiosqlite>=0.20.0", "# Image processing and thumbnails", "# Updated for security patches (CVE-2023-50447, CVE-2024-28219)", "pillow>=11.0.0", "# Safe file deletion (recycle bin support)", "send2trash>=1.8.0", "# File system monitoring for auto-reindexing", "watchdog>=2.1.0", "# Windows-specific: Windows Property System metadata support", "# Only required on Windows; gracefully skipped on other platforms", "pywin32>=300; sys_platform == 'win32'"]

[project.urls]
Repository = "https://github.com/MajoorWaldi/ComfyUI-Majoor-AssetsManager"
#  Used by Comfy Registry https://registry.comfy.org
Documentation = "https://github.com/MajoorWaldi/ComfyUI-Majoor-AssetsManager/wiki"
"Bug Tracker" = "https://github.com/MajoorWaldi/ComfyUI-Majoor-AssetsManager/issues"

[tool.comfy]
PublisherId = "majoor-waldi"
DisplayName = "ComfyUI-Majoor-AssetsManager"
Icon = "js/assets/icon.svg"
includes = [] 
# "requires-comfyui" = ">=1.0.0"  # ComfyUI version compatibility

