module.exports = {
  // Placeholder eslint config restored. Replace with original rules as needed.
  root: true,
  env: {
    node: true,
  },
  extends: [
    'eslint:recommended'
  ],
  rules: {},
};
