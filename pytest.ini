[pytest]
# Ne collecter que les tests de CE repo.
testpaths = tests
# Évite que l'archive d'ancienne version (et autres dossiers) perturbe sys.path / conftest.
norecursedirs = __old_version_zip .* build dist __pycache__ node_modules
addopts = -ra
